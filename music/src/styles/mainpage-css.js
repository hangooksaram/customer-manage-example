import styled from 'styled-components';

const Wrapper = styled.div`
  background : red;
`

const Title= styled.h1`
  background : yellow;
`
const textStyle = {
  color : 'blue'
}

export {Wrapper, Title, textStyle};